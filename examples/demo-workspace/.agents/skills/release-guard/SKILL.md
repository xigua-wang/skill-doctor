---
name: Release Guard
description: OpenClaw project agent release guard that should lose to workspace skills.
---

# Release Guard

Project agent skill used to validate OpenClaw `.agents/skills` precedence.

triggers:
- "openclaw release"
- "project fallback"
