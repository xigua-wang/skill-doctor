---
name: Release Guard
description: OpenClaw workspace-level release guard that should win over lower-precedence copies.
---

# Release Guard

OpenClaw workspace skill for release approval checks.

triggers:
- "openclaw release"
- "approve deploy"
